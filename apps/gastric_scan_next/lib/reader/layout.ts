/** Reader / workbench chrome sizes. Keep image canvas as the dominant area. */

export const READER_CASE_SIDEBAR_PX = 180;
export const READER_REPORT_SIDEBAR_PX = 256;
export const READER_EVIDENCE_MAX_H = 'min(340px, 42vh)';
export const READER_DEFAULT_MASK_OPACITY = 0.04;

/** Main workbench (`/` and `/workbench`) patient list + evidence drawer. */
export const WORKBENCH_PATIENT_LIST_REM = 15;
export const WORKBENCH_EVIDENCE_WIDTH = 'clamp(18rem, 24vw, 24rem)';
export const WORKBENCH_WALL_DOCK_WIDTH = 'clamp(22rem, 30vw, 30rem)';

/** Phone / small-tablet chrome: overlay sheets + bottom nav instead of 3 columns. */
export const WORKBENCH_MOBILE_MAX_PX = 767;
