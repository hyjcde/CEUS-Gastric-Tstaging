paths:
  sms_root: third_party/sms
  prepared_dataset_root: data/processed/sms/gastric_lumen_2d_baseline
  experiment_root: experiments/segmentation

experiment:
  task_name: segmentation
  model_alias: unet2d
  data_version: dataset_v20260408
  run_id: r001

train:
  model_name: unet_2d
  dimension: 2d
  in_channels: 1
  out_channels: 1
  loss_type: binary_2d
  optimizer_type: adamw
  scheduler_type: step_lr
  lr: 0.001
  batch: 8
  num_workers: 4
  num_epochs: 100
  save_every_epochs: 10
  vis_every: 200
  seed: 666
  ema: true
  image_size: 512

inference:
  save_probability: true
  calculate_sms_metrics: false
