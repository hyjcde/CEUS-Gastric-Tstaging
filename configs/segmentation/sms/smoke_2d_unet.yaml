paths:
  sms_root: third_party/sms
  prepared_dataset_root: data/processed/sms/smoke_internal_eval
  experiment_root: experiments/segmentation

experiment:
  task_name: segmentation
  model_alias: sms_smoke_unet2d
  data_version: smoke_v20260408
  run_id: r001

train:
  model_name: unet_2d
  dimension: 2d
  in_channels: 1
  out_channels: 1
  loss_type: binary_2d
  optimizer_type: adamw
  scheduler_type: step_lr
  lr: 0.001
  batch: 2
  num_workers: 0
  num_epochs: 1
  save_every_epochs: 1
  vis_every: 1
  seed: 666
  ema: true
  image_size: 256

inference:
  save_probability: true
  calculate_sms_metrics: false
