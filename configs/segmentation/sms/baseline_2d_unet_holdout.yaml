paths:
  sms_root: third_party/sms
  prepared_dataset_root: data/processed/sms/baseline_2d_unet_holdout_crop_ui
  experiment_root: experiments/segmentation

experiment:
  task_name: segmentation
  model_alias: unet2d_holdout_cropui
  data_version: dataset_v20260408_holdout_cropui
  run_id: r001

train:
  model_name: unet_2d
  dimension: 2d
  in_channels: 1
  out_channels: 1
  loss_type: binary_2d
  optimizer_type: adamw
  scheduler_type: step_lr
  lr: 0.001
  batch: 8
  num_workers: 4
  num_epochs: 50
  save_every_epochs: 5
  vis_every: 200
  seed: 666
  ema: true
  image_size: 512

inference:
  save_probability: true
  calculate_sms_metrics: false
